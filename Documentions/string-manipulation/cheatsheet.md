# here some function to manipulate string 

name | description | return
format|format string with {} | new string
lower|transform to lower case|
upper|transform to upper case 
strip| remove white space or string to string |
title| capitalize all words|
split| split string an convert to list | list
find | search in the strig| int
replace| search in string and replace with |


# join list 

To join list use a string as a pattern and call the it join method to join array 

# search in string 

use the `in` operator to search in 